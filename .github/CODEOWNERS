# Require review from the repository owner on every change before merge.
# Pair this with a branch ruleset on `main` that enables
# "Require a pull request before merging" + "Require review from Code Owners".
* @nambii
